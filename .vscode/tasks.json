{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "VibeOS: make disk",
      "type": "shell",
      "command": "make disk",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "VibeOS: build all",
      "type": "shell",
      "command": "make",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "VibeOS: run (QEMU + noVNC)",
      "type": "shell",
      "command": "./scripts/run-qemu.sh",
      "options": { "cwd": "${workspaceFolder}" },
      "isBackground": true,
      "problemMatcher": []
    }
  ]
}
